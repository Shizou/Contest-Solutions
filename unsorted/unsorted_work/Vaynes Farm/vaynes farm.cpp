#include<iostream>
#include<cstdio>
#include<algorithm>

using namespace std;

int T,G,N,P,M,D;
int h[100000];
int main(){
    freopen("input.txt","r",stdin);
    scanf("%d",&T);
    scanf("%d%d%d%d%d",&G,&N,&N,&P,&M,&D);
    for(int i = 0;i < N;i++){
        scanf("%d",&h[i]);
    }
    sort(h, h + N);
    for(int i = 0;G < ;i++){
        tmp+=

    }

}
