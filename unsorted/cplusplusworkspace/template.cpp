/*Libraries*/
#include<algorithm>
#include<cctype>
#include<cmath>
#include<cstdio>
#include<cstring>
#include<iostream>
#include<map>
#include<queue>
#include<set>
#include<sstream>
#include<stack>
#include<vector>
/*Misceallanous functions*/
#define to_s(i) static_cast<ostringstream*>( &(ostringstream() << i) )->str();
#define to_i(s) ({int _; stringstream SS; SS<<(s); if(!SS>>_)_ = 0;)_;})
#define r_int(i) scanf("%d",&i)
#define r_str(s) cin >> s
#define loop(i,N) for(i = i; i < N;++i)
#define pb push_back
#define mp make_pair
#define Open(I,O)if(I[0]) freopen(I,"r",stdin); if(O[0])freopen(O,"w",stdin);
using namespace std;

int N;
vector<int> s;
int main(){
    loop(5,100){

    }


    return 0;
}

