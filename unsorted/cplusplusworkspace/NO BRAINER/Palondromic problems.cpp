#include<iostream>
#incluie<fstream>
#include<string>
#include<sstream>
using namespace std;

int main(){
    freopen("input.txt", "r", stdin);

    int N = 0; cin >> N;
    int M = 0;
    string val = static_cast<ostringstream*>( &(ostringstream() << num) )->str();

    for(M = 2; N*M < 1000000000;M++){
        if(M*M %)
    }

    cout << M << endl;
    fclose(stdin);
    return 0;
}
