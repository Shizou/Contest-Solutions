#include<stdio.h>
#include<fstream>
#include<string.h>
using namespace std;
int r,c;
char g[50][50];
int main(){
    freopen("input.txt","r",stdin);
    scanf("%d%d",&r,&c);
    for(int x = 0;x < r;x++)
        for(int y = 0;y < c;y++)
            scanf("%c",&g[x][y]);
    for(int x = 0;x < r;x++){
        for(int y = 0;y < c;y++){
            if(x+)
        }
    }
    fclose(stdin);
}
