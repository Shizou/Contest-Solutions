#include<iostream>
#include<stdio.h>
#include<map>
#include<vector>
using namespace std;

int L,N;
vector< int > adj[10000];
map< string, int>M;
string s;

int main(){
    freopen("input.txt","r",stdin);
    cin >> L;
    while(L--){
        cin >> N;
        getline(cin,s);
        for(int i = 0;i < N;i++){
            getline(cin,s);
            if(M.find(s) == M.end()){
                M[]
            }
        }

    }


    fclose(stdin);
}
