##include<iostream>
#include<cstdio>
#include<cstring>
#include<queue>
#include<vector>
#include<algorithm>
#define pb push_back
#define ppb pop_back
#define mp make_pair
#define all(x) x.begin(),x.end()
using namespace std;

int N,M,ans;
int ph,pd;

int main(){
    freopen("input.txt","r",stdin);
    scanf("%d%d",&N,&M);
    for(int i = 0,h,d;i < M;i++){
        scanf("%d%d",&h,&d);
        if(i > 0){
            if(abs(p-ph)%2!=0){

            }
        }
        ph = h,pd = d;
    }
    return 0;
}